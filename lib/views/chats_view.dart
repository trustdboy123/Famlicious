import 'package:flutter/material.dart';

class ChatViews extends StatelessWidget {
  const ChatViews({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.blue,
    );
  }
}
